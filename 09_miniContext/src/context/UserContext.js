import React from "react";

const UserContext = React.createContext() //we created a context and it gives a Provider, which will provide the variable to other components where it is needed 

export default UserContext